<UserControl
    x:Class="HyPlayer.Controls.LyricItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="LyricPanel"
    VerticalAlignment="Stretch"
    d:DesignHeight="70"
    d:DesignWidth="500"
    DoubleTapped="LyricItem_OnDoubleTapped"
    Loaded="LyricPanel_Loaded"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:WrapPanel x:Name="WordLyricContainer" Grid.Row="0" HorizontalAlignment="Center"/>
        <TextBlock
            Name="TextBoxPureLyric"
            Grid.Row="0"
            controls:DockPanel.Dock="Top"
            CharacterSpacing="0"
            FontWeight="Bold"
            TextAlignment="Center"
            TextWrapping="WrapWholeWords" />
        <TextBlock
            Name="TextBoxTranslation"
            Grid.Row="1"
            controls:DockPanel.Dock="Top"
            CharacterSpacing="0"
            FontWeight="Bold"
            TextAlignment="Center"
            TextWrapping="WrapWholeWords" />
        <TextBlock
            Name="TextBoxSound"
            Grid.Row="2"
            controls:DockPanel.Dock="Bottom"
            FontSize="15"
            TextAlignment="Center"
            TextWrapping="WrapWholeWords" />
    </Grid>

</UserControl>