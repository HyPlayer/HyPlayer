<Page
    x:Class="HyPlayer.Features.Album.AlbumPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:controls1="using:HyPlayer.UI.Lists"
    xmlns:uiControls="using:HyPlayer.UI.Controls"
    NavigationCacheMode="Disabled">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls1:ContainerItemsView
            x:Name="SongContainer"
            Grid.Row="1"
            Margin="20,30,0,0"
            Container="{x:Bind ViewModel.Album, Mode=OneWay}">
            <controls1:ContainerItemsView.ListHeader>
                <controls:DockPanel
                    x:Name="GridPersonalInformation"
                    Grid.Row="0"
                    LastChildFill="True">
                    <Rectangle
                        x:Name="RectangleImage"
                        Width="200"
                        Height="200"
                        Margin="16"
                        VerticalAlignment="Top"
                        controls:DockPanel.Dock="Left"
                        RadiusX="12"
                        RadiusY="12">
                        <Rectangle.Fill>
                            <ImageBrush
                                x:Name="ImageRect"
                                ImageSource="{x:Bind ViewModel.SourceImage, Mode=OneWay}"
                                Stretch="UniformToFill" />
                        </Rectangle.Fill>
                    </Rectangle>
                    <StackPanel
                        x:Name="StackPanelUserInfo"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                        <TextBlock
                            x:Name="TextBoxAlbumName"
                            IsTextSelectionEnabled="True"
                            Style="{ThemeResource TitleTextBlockStyle}"
                            Text="{x:Bind ViewModel.Album.Name, Mode=OneWay}"
                            TextWrapping="Wrap" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock
                                Padding="0,8,0,4"
                                FontSize="16"
                                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Text="艺术家:" />
                            <HyperlinkButton
                                x:Name="TextBoxAuthor"
                                Click="TextBoxAuthor_OnTapped"
                                Content="{x:Bind ViewModel.AuthorString, Mode=OneWay}"
                                FontSize="16"
                                Style="{ThemeResource TextBlockButtonStyle}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                            <TextBlock
                                FontSize="16"
                                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Text="发行时间:" />
                            <TextBlock
                                x:Name="TextBlockPublishTime"
                                FontSize="16"
                                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Text="{x:Bind ViewModel.PublishTime, Mode=OneWay, Converter={StaticResource DateConverter}}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="15">
                            <Button Click="PlayAll_Click" Style="{ThemeResource AccentButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <FontIcon
                                        x:Name="IconPlay"
                                        FontSize="16"
                                        Glyph="&#xEDB5;" />
                                    <TextBlock Margin="4,0,0,0" Text="播放" />
                                </StackPanel>
                            </Button>
                            <Button Click="AddAll_Click" Content="添加全部" />
                            <Button Click="DownloadAll_Click" Content="下载全部" />
                            <ToggleButton
                                x:Name="BtnSub"
                                Command="{x:Bind ViewModel.SubscribeCommand}"
                                Content="收藏" />
                            <Button Command="{x:Bind ViewModel.NavigateCommentCommand}" Content="评论" />
                        </StackPanel>
                        <uiControls:ExpandableTextBox
                            x:Name="TextBlockDesc"
                            MaxWidth="700"
                            Margin="0,8,0,0"
                            HorizontalAlignment="Left"
                            FontSize="14"
                            MaxLines="7"
                            Text="{x:Bind ViewModel.Description, Mode=OneWay}"
                            TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </controls:DockPanel>
            </controls1:ContainerItemsView.ListHeader>
        </controls1:ContainerItemsView>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RectangleImage.Visibility" Value="Collapsed" />
                        <Setter Target="StackPanelUserInfo.(Grid.Column)" Value="1" />
                        <Setter Target="StackPanelUserInfo.(Grid.ColumnSpan)" Value="10" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1024" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RectangleImage.Visibility" Value="Visible" />
                        <Setter Target="StackPanelUserInfo.(Grid.Column)" Value="5" />
                        <Setter Target="StackPanelUserInfo.(Grid.ColumnSpan)" Value="6" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>


</Page>
