<Page
    x:Class="HyPlayer.Features.Library.MusicCloudPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:controls1="using:HyPlayer.UI.Lists"
    NavigationCacheMode="Disabled">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:DockPanel LastChildFill="True">
            <Button
                x:Name="BtnUpload"
                Margin="0,-5,30,0"
                HorizontalAlignment="Right"
                controls:DockPanel.Dock="Right"
                Click="BtnUpload_Click"
                Content="上传本地" />
            <Button
                x:Name="BtnRefresh"
                Margin="0,-5,30,0"
                HorizontalAlignment="Right"
                controls:DockPanel.Dock="Right"
                Click="BtnRefresh_OnClick"
                Content="刷新列表" />
            <TextBlock
                Margin="30,30,35,0"
                controls:DockPanel.Dock="Left"
                FontSize="40"
                FontWeight="Bold"
                Text="我的云盘"
                TextWrapping="Wrap" />
        </controls:DockPanel>

        <controls1:ContainerItemsView
            x:Name="SongContainer"
            Grid.Row="1"
            Container="{x:Bind CloudContainer, Mode=OneWay}"
            ExtraItemActions="{x:Bind ItemActions, Mode=OneWay}"
            GreedyLoad="{x:Bind GreedyLoad, Mode=OneWay}"
            IsSearchEnabled="True" />
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RectangleImage.Visibility" Value="Collapsed" />
                        <Setter Target="StackPanelUserInfo.(Grid.Column)" Value="1" />
                        <Setter Target="StackPanelUserInfo.(Grid.ColumnSpan)" Value="10" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1024" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RectangleImage.Visibility" Value="Visible" />
                        <Setter Target="StackPanelUserInfo.(Grid.Column)" Value="5" />
                        <Setter Target="StackPanelUserInfo.(Grid.ColumnSpan)" Value="6" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>


</Page>