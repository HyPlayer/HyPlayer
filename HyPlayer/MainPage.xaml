<Page
    x:Class="HyPlayer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls2="using:HyPlayer.UI.Playback.PlayBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hyPlayerPage="using:HyPlayer.Shell"
    xmlns:hyplayer="using:HyPlayer.Platform.Xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    PointerMoved="Page_PointerMoved"
    mc:Ignorable="d">
    <Page.Resources>
        <LinearGradientBrush x:Key="OpacityGradientBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0" Color="{Binding Source={ThemeResource GradientColor}, Converter={StaticResource TransparentColorConverter}}" />
            <GradientStop Offset="1" Color="{ThemeResource GradientColor}" />
        </LinearGradientBrush>
        <Storyboard
            x:Key="ImageResetPositionAni"
            x:Name="ImageResetPositionAni"
            x:FieldModifier="public">
            <DoubleAnimation
                Storyboard.TargetName="ExpandedPlayerPositionOffset"
                Storyboard.TargetProperty="X"
                To="0"
                Duration="0:0:0.25" />
            <DoubleAnimation
                Storyboard.TargetName="ExpandedPlayerPositionOffset"
                Storyboard.TargetProperty="Y"
                To="0"
                Duration="0:0:0.25" />
        </Storyboard>
        <Storyboard x:Name="PointerInAni">
            <DoubleAnimation
                Storyboard.TargetName="GridPlayBar"
                Storyboard.TargetProperty="Opacity"
                To="1"
                Duration="00:00:00.55">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                Storyboard.TargetName="GridPlayBarMarginBlur"
                Storyboard.TargetProperty="Opacity"
                To="1"
                Duration="00:00:00.55">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                Storyboard.TargetName="PlayBarTrans"
                Storyboard.TargetProperty="Y"
                To="0"
                Duration="00:00:00.55">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

        </Storyboard>
    </Page.Resources>
    <Grid>
        <Frame
            Name="MainFrame"
            x:FieldModifier="public" />
        <ContentControl
            Name="ExpandedPlayer"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            x:FieldModifier="public"
            Visibility="Collapsed">
            <ContentControl.RenderTransform>
                <TranslateTransform x:Name="ExpandedPlayerPositionOffset" x:FieldModifier="public" />
            </ContentControl.RenderTransform>
        </ContentControl>
        <Grid
            Name="GridPlayBarMarginBlur"
            Height="110"
            Margin="0,10,0,0"
            VerticalAlignment="Bottom"
            x:FieldModifier="public"
            Visibility="Visible">
            <Rectangle
                x:Name="PlayBarMarginBackground"
                Grid.Column="0"
                Grid.ColumnSpan="4"
                Fill="{ThemeResource OpacityGradientBrush}"
                Opacity="1" />
        </Grid>
        <Grid
            Name="GridPlayBar"
            Height="120"
            Margin="{x:Bind hyplayer:XamlHelpers.Setting.playBarMargin, Mode=OneWay, Converter={StaticResource PlayBarMarginConverter}}"
            VerticalAlignment="Bottom"
            x:FieldModifier="public"
            Background="{StaticResource SystemControlAcrylicElementMediumHighBrush}"
            BorderBrush="{ThemeResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="{x:Bind hyplayer:XamlHelpers.Setting.playBarMargin, Mode=OneWay, Converter={StaticResource PlayBarCornerRadiusConverter}}"
            Opacity="1"
            Visibility="Visible">
            <controls2:PlayBar />
            <Grid.RenderTransform>
                <TranslateTransform x:Name="PlayBarTrans" />
            </Grid.RenderTransform>
        </Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowHeight="300" />
                        <AdaptiveTrigger MinWindowHeight="300" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="GridPlayBar.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowHeight="0" />
                        <AdaptiveTrigger MinWindowHeight="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="GridPlayBar.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
